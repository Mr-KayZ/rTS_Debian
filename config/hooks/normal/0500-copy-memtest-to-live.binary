#!/bin/sh

set -e

# Copy memtest86+ files from chroot to /live directory in the ISO
if [ -f chroot/boot/memtest86+x64.bin ]; then
    cp chroot/boot/memtest86+x64.bin binary/live/
fi

if [ -f chroot/boot/memtest86+x64.efi ]; then
    cp chroot/boot/memtest86+x64.efi binary/live/
fi
